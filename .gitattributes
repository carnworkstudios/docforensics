# No weights are tracked here (see .gitignore). This is in place for the day
# that changes, so a large file can never land outside LFS by accident.
*.onnx filter=lfs diff=lfs merge=lfs -text
*.pt   filter=lfs diff=lfs merge=lfs -text
*.pdf  filter=lfs diff=lfs merge=lfs -text

# Not built yet

`docforensics` on PyPI (checked via
`https://pypi.org/pypi/docforensics/json`, is not built yet.

When there is a package, it goes here and gets a `push.sh` beside the other
two. Until then there is nothing to publish and nothing to configure.

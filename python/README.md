# Not built yet

`docforensics` is unclaimed on PyPI (checked via
`https://pypi.org/pypi/docforensics/json`, which returns a real 404 — the
`/project/` URL returns 200 for names that do not exist, so do not use it).

When there is a package, it goes here and gets a `push.sh` beside the other
two. Until then there is nothing to publish and nothing to configure.
